import { State } from "./State";

export class GameState extends State {
    constructor() {
        super();
        console.log("SHIET");
    }
    click = (event: MouseEvent) => {
    }
    render = () => { };
    update = () => { };
}